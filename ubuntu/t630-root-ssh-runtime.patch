--- a/common-session
+++ b/common-session
@@ -29,2 +29,5 @@
 session	required	pam_unix.so 
+# Root SSH must not manage the separate physical Weston's runtime directory.
+# Only session bookkeeping is skipped; authentication/account checks remain.
+session [success=1 default=ignore] pam_succeed_if.so service = sshd uid = 0 quiet
 session	optional	pam_systemd.so 
